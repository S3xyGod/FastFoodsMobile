import React from 'react';
import {StyleSheet} from 'react-native';
import size from '../../untils/size';
export const styles = StyleSheet.create({
  touch: {
    height: size.REAL_SIZE_30,
    width: size.REAL_SIZE_30,
    borderRadius: 20,
  },
});
